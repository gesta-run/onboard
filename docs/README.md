# Gesta Onboard Assets

This directory is published by GitHub Pages.

```sh
curl -fsSL https://gesta-run.github.io/onboard/install-agent.sh | bash -s -- \
  --control-url https://console.gesta.run \
  --apikey sk-...
```
